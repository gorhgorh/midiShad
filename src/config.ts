/** Project-wide settings */
export const config = {
  /** Seconds of mouse inactivity before UI fades out */
  infoFadeTimeout: 3,
  /** Fade transition duration in CSS seconds */
  infoFadeDuration: 0.6,
} as const
